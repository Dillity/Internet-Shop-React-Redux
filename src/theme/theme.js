import {createTheme} from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#181818"
        },
        background: {
            paper: "#EEEEEE"
        }
    },
    typography: {
        fontFamily: "Work Sans"
    }
})